export function superFunction(caller: string) {
    console.log(`Super function called by: ${caller} from ${__dirname}!`)
}

export function turboFunction(caller: string) {
    console.log(`Turbo function called by: ${caller} from ${__dirname}!`)
}